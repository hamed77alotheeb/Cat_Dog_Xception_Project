
# تقرير مشروع تصنيف القطط والكلاب باستخدام التعلم العميق

## 1. مقدمة

أصبح تصنيف الصور باستخدام تقنيات التعلم العميق من التطبيقات المهمة في مجال الرؤية الحاسوبية، حيث يمكن للنماذج الحديثة استخراج الخصائص البصرية من الصور والتعرف على الأنماط الموجودة فيها.

يهدف هذا المشروع إلى تطوير نظام قادر على تصنيف صور الحيوانات إلى فئتين رئيسيتين، وهما القطط والكلاب، باستخدام نموذج تعلم عميق متقدم. تم اختيار نموذج Xception المدرب مسبقًا على قاعدة بيانات ImageNet، ثم الاستفادة من Transfer Learning وFine-Tuning لتدريب النموذج على المهمة المطلوبة.

بالإضافة إلى النموذج، تم تطوير واجهة استخدام باستخدام Gradio تسمح للمستخدم بإدخال صورة جديدة والحصول على الفئة المتوقعة ودرجة الثقة.

---

## 2. مشكلة المشروع

تكمن مشكلة المشروع في بناء نظام يستطيع التمييز بين صور القطط والكلاب بصورة دقيقة، حتى مع اختلاف الصور في الإضاءة والخلفية وزاوية التصوير والحجم.

ويحتاج هذا النوع من الأنظمة إلى نموذج قادر على استخراج خصائص بصرية مهمة مثل شكل الوجه والعينين والأذنين والفرو وبنية الجسم وغيرها من الخصائص التي تساعد في التمييز بين الفئتين.

---

## 3. أهداف المشروع

يهدف المشروع إلى:

1. بناء نموذج تعلم عميق لتصنيف صور القطط والكلاب.
2. استخدام نموذج Xception المدرب مسبقًا على ImageNet.
3. تطبيق Transfer Learning للاستفادة من الخصائص التي تعلمها النموذج مسبقًا.
4. استخدام Fine-Tuning لتحسين النموذج على بيانات المشروع.
5. تطبيق Data Augmentation لتحسين قدرة النموذج على التعميم.
6. تقييم النموذج باستخدام Validation وTest.
7. إنشاء واجهة استخدام تسمح للمستخدم باختبار صور جديدة بسهولة.

---

## 4. البيانات المستخدمة

تم استخدام مجموعة بيانات مكونة من 1852 صورة للتدريب والتحقق.

توزيع البيانات:

| الفئة | عدد الصور |
|---|---:|
| Cats | 924 |
| Dogs | 928 |
| الإجمالي | 1852 |

تم تقسيم البيانات إلى:

| المجموعة | عدد الصور |
|---|---:|
| Training | 1482 |
| Validation | 370 |

أي أن التقسيم المستخدم كان:

- 80% Training
- 20% Validation

كما تم توفير مجموعة اختبار مستقلة لم يتم استخدامها أثناء تدريب النموذج، وتتكون من 54 صورة:

| الفئة | عدد الصور |
|---|---:|
| Cats | 27 |
| Dogs | 27 |
| الإجمالي | 54 |

وجود مجموعة اختبار مستقلة يساعد على تقييم النموذج على صور لم تدخل في التدريب.

---

## 5. تجهيز البيانات

تم تغيير حجم الصور إلى:

**299 × 299 بكسل**

وتم استخدام Data Augmentation أثناء التدريب، ومن أهم التحويلات المستخدمة:

- Random Flip
- Random Rotation
- Random Zoom
- Random Translation
- Random Contrast

تهدف هذه العمليات إلى زيادة تنوع الصور التي يراها النموذج أثناء التدريب وتقليل اعتماد النموذج على تفاصيل ثابتة في الصور الأصلية.

---

## 6. النموذج المستخدم

تم استخدام نموذج:

**Xception**

وهو نموذج عميق مدرب مسبقًا على ImageNet.

تم استخدام النموذج كأساس لاستخراج الخصائص البصرية، ثم إضافة طبقات خاصة بالمشروع للتصنيف الثنائي.

يتكون الجزء المضاف من النموذج من:

- Global Average Pooling
- Batch Normalization
- Dropout
- Dense Layer
- Dropout
- Sigmoid Output Layer

طبقة الإخراج تستخدم Sigmoid لأن المهمة تتكون من فئتين فقط:

- Cat
- Dog

---

## 7. Transfer Learning

في المرحلة الأولى من التدريب تم تجميد معظم طبقات Xception واستخدامها كمستخرج للخصائص.

تم تدريب الطبقات النهائية الخاصة بالمشروع على بيانات القطط والكلاب.

يساعد Transfer Learning على الاستفادة من الخصائص العامة التي تعلمها النموذج مسبقًا من ImageNet، مما يقلل الحاجة إلى تدريب نموذج كامل من الصفر.

---

## 8. Fine-Tuning

بعد المرحلة الأولى تم إجراء Fine-Tuning للنموذج.

تم فتح عدد من الطبقات الأخيرة في Xception للتدريب باستخدام معدل تعلم صغير.

كما تم إبقاء طبقات Batch Normalization مجمدة للمساعدة على استقرار التدريب.

تم استخدام:

**Adam Optimizer**

مع معدل تعلم صغير أثناء Fine-Tuning.

---

## 9. تقنيات منع فرط التوافق

تم استخدام عدة تقنيات للمساعدة على تقليل Overfitting، منها:

- Data Augmentation
- Dropout
- Early Stopping
- ReduceLROnPlateau
- Model Checkpoint
- Fine-Tuning بمعدل تعلم منخفض

كما تم استخدام Validation Set لمتابعة أداء النموذج أثناء التدريب.

---

## 10. دوال الخسارة ومقاييس التقييم

تم استخدام:

**Binary Crossentropy**

كدالة خسارة لأن المشكلة هي تصنيف ثنائي.

كما تم استخدام المقاييس التالية:

### Accuracy

تقيس نسبة التوقعات الصحيحة من إجمالي التوقعات.

### Precision

تقيس نسبة التوقعات الموجبة الصحيحة من جميع التوقعات الموجبة.

### Recall

تقيس قدرة النموذج على اكتشاف الحالات الصحيحة من إجمالي الحالات الفعلية.

---

## 11. نتائج التدريب

حقق النموذج النهائي نتائج مرتفعة على مجموعة Validation.

### Validation

| المقياس | النتيجة |
|---|---:|
| Accuracy | 99.73% |
| Precision | 100.00% |
| Recall | 99.73% |
| Loss | 0.0125 |

---

## 12. نتائج الاختبار

تم اختبار النموذج باستخدام مجموعة اختبار مستقلة تحتوي على 54 صورة.

النتائج:

| المقياس | النتيجة |
|---|---:|
| Accuracy | 100.00% |
| Precision | 100.00% |
| Recall | 100.00% |
| Loss | 0.0018 |

وكان عدد التوقعات الصحيحة:

**54 من أصل 54 صورة**

وكان عدد الأخطاء:

**0**

---

## 13. اختبار صورة خارجية

بعد الانتهاء من التدريب، تم اختبار النموذج على صورة قطة خارج مجموعة التدريب والاختبار.

كانت نتيجة النموذج:

```text
CAT: 95.39%
DOG: 4.61%
````

هذه النتيجة توضح أن النموذج تمكن من التعرف على الصورة الخارجية كقطة بدرجة ثقة مرتفعة.

وتختلف درجة الثقة من صورة إلى أخرى حسب خصائص الصورة ومدى تشابهها مع الصور التي تعلم منها النموذج.

---

## 14. واجهة المستخدم

تم تطوير واجهة استخدام باستخدام مكتبة Gradio.

تسمح الواجهة للمستخدم برفع صورة ثم يقوم النموذج بتحليلها.

تظهر الواجهة:

* الفئة المتوقعة.
* درجة الثقة.
* احتمال Cat.
* احتمال Dog.

تشغيل الواجهة يتم باستخدام:

```bash
python app.py
```

---

## 15. الفرق بين دقة النموذج ودرجة الثقة

من المهم التمييز بين Accuracy وConfidence.

Accuracy هي مقياس لأداء النموذج على مجموعة من الصور أثناء التقييم.

أما Confidence فهي درجة ثقة النموذج في تنبؤ واحد لصورة محددة.

على سبيل المثال، قد يكون:

```text
Validation Accuracy = 99.73%
```

بينما يعطي النموذج لصورة معينة:

```text
CAT = 95.39%
```

وهذا أمر طبيعي ولا يعني وجود تناقض بين القيمتين.

---

## 16. مناقشة النتائج

تشير النتائج إلى أن النموذج تعلم الخصائص البصرية المهمة للتمييز بين القطط والكلاب بصورة جيدة.

وصول Validation Accuracy إلى 99.73% يشير إلى أداء مرتفع على مجموعة التحقق.

كما حقق النموذج 100% على مجموعة الاختبار الحالية المكونة من 54 صورة.

ومع ذلك، يجب تفسير نتيجة الاختبار بحذر بسبب العدد المحدود نسبيًا من صور الاختبار. لذلك لا يمكن اعتبار النتيجة ضمانًا بأن النموذج سيحقق 100% على جميع الصور الجديدة.

كما أن الصور الواقعية قد تختلف عن بيانات التدريب في عدة جوانب مثل:

* الإضاءة.
* الخلفية.
* زاوية التصوير.
* جودة الصورة.
* حجم الحيوان في الصورة.
* اختلاف مصدر الصور.

---

## 17. حدود المشروع

من أهم حدود المشروع:

1. حجم مجموعة الاختبار المستقلة محدود.
2. قد تختلف الصور الجديدة عن صور التدريب في الشكل والبيئة.
3. درجة الثقة ليست ضمانًا لصحة التنبؤ.
4. يمكن أن توجد صور صعبة بصريًا قد يكون التمييز فيها أكثر صعوبة.
5. يحتاج تقييم التعميم الحقيقي إلى اختبار أكبر وأكثر تنوعًا.

---

## 18. التحسينات المستقبلية

يمكن تطوير المشروع مستقبلاً من خلال:

* زيادة حجم بيانات التدريب.
* زيادة تنوع الصور.
* استخدام مجموعة اختبار أكبر.
* استخدام بيانات من مصادر متعددة.
* دراسة Calibration لدرجات الثقة.
* تحسين واجهة المستخدم.
* نشر النظام كتطبيق ويب.
* إضافة فئات حيوانات أخرى.

---

## 19. خلاصة المشروع

تم في هذا المشروع بناء نظام لتصنيف صور القطط والكلاب باستخدام نموذج Xception وتقنيات Transfer Learning وFine-Tuning.

تم تدريب النموذج باستخدام 1852 صورة، بالإضافة إلى مجموعة اختبار مستقلة مكونة من 54 صورة.

حقق النموذج النهائي:

**99.73% Validation Accuracy**

و:

**100% Test Accuracy**

مع:

**100% Precision**

و:

**100% Recall**

كما تم تطوير واجهة باستخدام Gradio تسمح للمستخدم باختبار صور جديدة بسهولة.

توضح النتائج أن استخدام Transfer Learning مع Xception كان مناسبًا لمهمة تصنيف القطط والكلاب، مع الحصول على أداء مرتفع على بيانات التقييم المستخدمة في المشروع.

---

## 20. معلومات المشروع

**اسم المشروع:**
Cat vs Dog Image Classification using Xception

**اسم الطالب:**
حميد حسين محمد العذيب 

**اسم الجامعة / الكلية:**
كلية المجتمع _صنعاء

**اسم المقرر:**
image processing

**اسم الدكتور:**
د\عبدالله يحيى محمد معاذ

**تاريخ التسليم:**
2026/9/16
